// import { ErrorRequestHandler, NextFunction, Request, Response } from "express";

// const validationError = function (err:ErrorRequestHandler, req:Request, res:Response, next: NextFunction ) {
// if(err.message == "")
// }
